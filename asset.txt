 {/* Use the Button component and pass props */}
        {/* <Button
          label={loading ? 'Loading...' : 'Make API Call'}
          onClick={handleApiCall}
          isLoading={loading}
        /> */}